{
  "name": "shift-clock-reminders",
  "private": true,
  "type": "module",
  "scripts": {
    "send-reminders": "node scripts/send-shift-reminders.mjs",
    "android:web": "node scripts/prepare-android-web.mjs",
    "android:add": "npx cap add android",
    "android:sync": "npm run android:web && npx cap sync android",
    "android:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.3",
    "@capacitor/core": "^7.4.3",
    "@capacitor-firebase/authentication": "^7.4.0",
    "firebase": "^12.0.0",
    "firebase-admin": "^13.4.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.3",
    "typescript": "^5.9.2"
  }
}
